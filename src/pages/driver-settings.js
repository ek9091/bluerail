import React from "react";

import { AppLayout as Layout } from "../lib/app/ui-components";

export const Vehicles = () => {
  return (
    <Layout>
      This is where a driver will be able to adjust their settings and
      subscription
    </Layout>
  );
};

export default Vehicles;
