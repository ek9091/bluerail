import React from "react";

import { AppLayout as Layout } from "../lib/app/ui-components";

export const History = () => {
  return <Layout>This is where a driver can view previous rides</Layout>;
};

export default History;
