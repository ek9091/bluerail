import React from "react";

import { AppLayout as Layout } from "../../lib/app/ui-components";

export const Applications = () => {
  return <Layout>This is area where an employee can approve drivers</Layout>;
};

export default Applications;
