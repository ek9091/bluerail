import React from "react";

import { AppLayout as Layout } from "../../lib/app/ui-components";

export const SingleApplication = () => {
  return (
    <Layout>
      <p>This is a single application</p>
    </Layout>
  );
};

export default SingleApplication;
