import React from "react";

import { AppLayout } from "../lib/app/ui-components";

export const Rides = () => {
  return (
    <AppLayout>This is where a driver will view their current rides</AppLayout>
  );
};

export default Rides;
