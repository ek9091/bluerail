import React from "react";

import { AppLayout as Layout } from "../lib/app/ui-components";

export const DriverApplication = () => {
  return (
    <Layout title="Become a Driver">
      <p>Become a driver page</p>
    </Layout>
  );
};

export default DriverApplication;
