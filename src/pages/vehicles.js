import React from "react";

import { AppLayout as Layout } from "../lib/app/ui-components";

export const Vehicles = () => {
  return (
    <Layout>
      this is where a driver will be able to view their Vehicles and register
      more vehicles
    </Layout>
  );
};

export default Vehicles;
