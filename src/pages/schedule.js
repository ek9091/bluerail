import React from "react";

import { AppLayout as Layout } from "../lib/app/ui-components";

export const Schedule = () => {
  return (
    <Layout>this is where a drive will be able to view their schedule</Layout>
  );
};

export default Schedule;
